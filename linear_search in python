#linear search in python 


lst = [3,25,65,6,4,7,8,45,87,12,56,78,90,49]
lst.sort()

l = 0 
u = len(lst) - 1

m = (l +u)//2

search = int(input("enter an number"))

if search in lst:
	a = 1
	while a < 2:
	
		if search == lst[m]:
			print(search, "index is ", m)
			a += 1
		elif lst[m] < search :
			l = m 
			m = (l+u)//2
		elif lst[m] > search:
			u = m
			m = (l+u)//2
else:
	print("the number doesn't exist in the list")
	
	
